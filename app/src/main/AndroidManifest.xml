<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.survirun">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Survirun">
        <activity
            android:name=".activity.exercise.ExerciseResultActivity"
            android:exported="true" />
        <activity
            android:name=".activity.ExplanationActivity"
            android:theme="@android:style/Theme.Dialog"></activity>
        <activity
            android:name=".activity.ExercisePreparationActivity"
            android:exported="true" />
        <activity android:name=".activity.exercise.ResultActivity" />
        <activity android:name=".activity.UserGoalModifyActivity" />
        <activity android:name=".activity.UserGoalActivity" />
        <activity android:name=".activity.exercise.MapActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBcD4zxjZ4BRHDN4xgOhAdG21TQPEtCRG0" />

        <activity android:name=".activity.account.SingUpProfileActivity" />
        <activity android:name=".activity.account.SingUpNameActivity" />
        <activity android:name=".activity.account.SingUpActivity" />
        <activity android:name=".activity.account.LoginActivity" />
        <activity android:name=".activity.account.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
    </application>

</manifest>